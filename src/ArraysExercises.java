import javafx.util.converter.PercentageStringConverter;

import java.util.Arrays;

/*
public class ArraysExercises {

    public static void main(String[] args) {
        int[] numbers = {1, 2, 3, 4, 5};
        System.out.println(numbers);

        System.out.println(Arrays.toString(numbers));
        Person people[] = new Person[3];
        people[0] = new Person("Jon Lennon");

        people[1] = new Person("Patricio ");

        people[2] = new Person("Ringo Star");

        for (Person person : people) {
            System.out.println(person.getName());
        }
        people = addPerson(people, new Person("Markle Tambourineman"));
        for (Person person : people) {
            System.out.println(person.getName());
        }
    }

    public static Person[] addPerson(Person[] personArr, Person personToAdd) {
        Person[] newPersonArray = Arrays.copyOf(personArr, personArr.length + 1);
        newPersonArray[newPersonArray.length - 1] = personToAdd;
        return newPersonArray;

    }
}
*/
