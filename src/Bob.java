

import java.util.Scanner;
/*
public class Bob {
    public static void main(String[] args) {
//keep scanner inside main method for now.
        //scanners are a type of object built in java to get user-input
        //must define type if stored in a variable. scanner may be noted as sc /input.
        // we are building a new object.  class says you have to pass in the property of the system class
        //constructors are a special function to build an object.  new Scanner builds an object. may need other materials.
        //scanners have set values.  priority use is input for a user.  can read from other files.  help parse .csv files.
        //buffered reader for .csv files.  files class.  parsing .csv.
        //"You can't parse shit without loops" -Ry    Name scanner general in case of function re=use.  sys.in gets input, sys.out prints out
        Scanner scanner = new Scanner(System.in);
        boolean bobTalks = true;
        do {
            System.out.println("Bob is a teenager. Teens are jerks.  Bearing that in mind, go ahead, ask Bob a question: ");
            String userInput = scanner.nextLine();
            if (userInput.endsWith("?")) {
                System.out.println("Sure.");
            } else if (userInput.endsWith("!")) {
                System.out.println("Whoah, chill out!");
            } else if (userInput.endsWith(" ")) {
                System.out.println("Fine. Be that way!");
            } else {
                System.out.println("Whatever");
            }
            System.out.println("Do you want to ask another question?");
            String userResponse = scanner.next();
            if (userResponse.equalsIgnoreCase("no")) {
                bobTalks = false;
            }scanner.nextLine();
        } while (bobTalks);
    }
}

*/
